#!/bin/bash

UNAME=$(uname)

successInstall() {
    corgit="\e[1mcorgit\e[0m"
    cgit="\e[1mcgit h\e[0m"

    echo -e "$corgit was installed successfully 🤩, type $cgit"
}

if [[ "$UNAME" == Linux || "$UNAME" == "Darwin" ]]; then
    sudo cp cgit /usr/local/bin

    successInstall

elif [[ "$UNAME" == CYGWIN* || "$UNAME" == MINGW* ]]; then
    cp cgit /usr/bin

    successInstall

else
    abdfnx="\e[1mabdfnx\e[0m"
    echo "😕, Unidentified system..."
    echo -e "support $abdfnx to add corgit to this system"
fi
